# Vertical Agent Platform Security Architecture

작성 기준일: 2026-06-22

## 1. 목표

이 문서는 직업군별 Windows exe + 구독형 라이선스 SaaS에서, **민감한 업무 데이터는 로컬에 두고 서버는 최소 역할만 맡기는 구조**를 정의한다.

핵심 목표는 다음과 같다.
- 회계/세무/법률처럼 민감정보가 많은 업종에서 신뢰를 확보한다.
- 서버 유출 위험과 운영 부담을 줄인다.
- 고객에게 "문서와 메모는 내 PC 안에만 저장된다"는 설명이 가능하게 만든다.
- 라이선스, 결제, 업데이트, 사용량 통계만 서버에서 관리한다.

---

## 2. 기본 원칙

### 원칙 1: 민감 데이터는 로컬 우선
- 사건 메모
- 거래처/의뢰인 메모
- 업로드한 파일 원본
- 생성된 초안 문서
- 검색용 인덱스
- 작업 로그

이 항목들은 기본적으로 사용자의 데스크톱 내부에 저장한다.

### 원칙 2: 서버는 컨트롤 플레인만 담당
서버는 다음만 책임진다.
- 로그인
- 라이선스 검증
- 결제 상태
- 좌석/기기 수 제한
- 모델 라우팅
- 최소 사용량 메타데이터
- 업데이트 배포

### 원칙 3: 사용자는 키를 넣지 않는다
비개발자 고객에게 API 키 입력을 강제하지 않는다.
기본 플로우는 설치 → 로그인 → 직군 선택 → 사용 이다.

### 원칙 4: 데이터 최소 수집
업무상 필요한 최소 정보만 저장하고, 나머지는 로컬에 둔다.

---

## 3. 권장 데이터 분리

| 데이터 종류 | 저장 위치 | 이유 |
|---|---|---|
| 계정 정보 | 서버 | 로그인/라이선스 필요 |
| 구독 상태 | 서버 | 결제 검증 필요 |
| 좌석/기기 등록 | 서버 | 공유 방지 필요 |
| 민감 문서 원본 | 로컬 | 개인정보/업무 비밀 보호 |
| 상담 메모 | 로컬 | 가장 민감한 업무 데이터 |
| 초안/결과물 | 로컬 | 재사용/수정 편의 |
| 검색 인덱스 | 로컬 | 빠른 탐색, 비공개 유지 |
| 최소 사용량 메타 | 서버 + 로컬 | 과금/한도 관리 |
| 백업(선택) | 사용자 선택 클라우드 | 필요 시만 동기화 |

---

## 4. 시스템 구조

```text
[Windows exe / Flutter]
        │
        ▼
[Local Vault]
  ├─ SQLite / encrypted local DB
  ├─ document cache
  ├─ drafts
  └─ search index
        │
        ├──► [Sync Layer] (optional)
        │        ├─ backup export
        │        ├─ encrypted sync
        │        └─ device migration
        │
        ▼
[FastAPI Control Plane]
  ├─ auth
  ├─ license check
  ├─ seat management
  ├─ billing
  ├─ pack delivery
  ├─ model routing
  └─ minimal usage ledger
        │
        └──► [LLM Provider / Router]
                 ├─ OpenRouter
                 ├─ direct provider API
                 └─ fallback provider
```

---

## 5. 로컬 저장소 설계

### 5-1. 권장 저장소
- **SQLite**: 기본 로컬 DB
- **암호화 레이어**: 민감 필드 보호
- **파일 시스템 vault**: 원본 파일/초안/이미지 저장

### 5-2. 저장 단위
- `profiles` : 직군 프로필, 사용자 설정
- `clients` : 거래처/의뢰인 메타
- `notes` : 메모
- `documents` : 파일 메타데이터
- `drafts` : 생성된 초안
- `tasks` : 체크리스트/해야 할 일
- `search_index` : 로컬 검색용 인덱스
- `sync_queue` : 서버 동기화 대기 항목

### 5-3. 로컬 저장의 장점
- 민감 정보가 밖으로 덜 나감
- 오프라인에서도 일부 기능 가능
- 법률/회계 고객에게 설명이 쉬움
- 서버 장애가 있어도 작업 메모가 남음

### 5-4. 로컬 저장의 단점
- 기기 변경 시 이전 필요
- 백업/복구 설계 필요
- 다중 PC 동기화가 복잡함

---

## 6. 서버 저장소 설계

서버는 사용자 문서 전체를 보관하지 않는 방향을 기본으로 한다.

### 서버가 보관하는 것
- 사용자 계정 식별자
- 라이선스 토큰
- 좌석/기기 등록 정보
- 결제 내역
- 최소 사용량 통계
- pack 버전 정보
- 업데이트 상태

### 서버가 보관하지 않는 것
- 민감 문서 원문
- 상담 메모 원문
- 고객 파일 원본
- 생성된 초안 전체

### 예외
사용자가 명시적으로 동기화/백업을 켠 경우에만, 암호화된 상태로 제한 저장할 수 있다.

---

## 7. 보안 위협 모델

### 위협 1: 사무실 내 exe 복사 공유
**대응**
- 로그인 필수
- 기기 등록
- 좌석 제한
- 주기적 라이선스 검증
- 동시 세션 제한

### 위협 2: 로컬 파일 유출
**대응**
- 로컬 DB 암호화
- 파일 vault 분리
- OS 계정 보호 안내
- 선택적 앱 잠금(PIN/Windows Hello)

### 위협 3: 서버 침해
**대응**
- 민감 문서 미저장
- 최소 메타데이터만 저장
- 토큰/라이선스만 보관
- 감사 로그 최소화

### 위협 4: LLM로 민감정보 유출
**대응**
- 전송 전 민감정보 마스킹 옵션
- 필요 시 사용자 확인 후 전송
- 프롬프트에서 원문 최소화
- 장문 문서는 chunking 후 요약

### 위협 5: 기기 변경/분실
**대응**
- 백업/내보내기
- 장치 재등록 플로우
- 관리자 승인 기반 seat 이동

---

## 8. 네트워크 정책

### 기본 정책
- 앱과 서버 통신은 HTTPS 전용
- cleartext HTTP는 개발/로컬 테스트 외 금지
- 민감한 파일 전송은 기본적으로 하지 않음

### 로컬-우선 연결
앱은 다음 순서로 동작한다.
1. 로컬 데이터 조회
2. 로컬 캐시 결과 우선 표시
3. 서버에서 라이선스/업데이트 확인
4. 필요한 경우에만 LLM 호출

---

## 9. LLM 호출 정책

### 입력 최소화
LLM에는 다음만 보낸다.
- 정규화된 작업 요청
- 필요한 최소 메타데이터
- 직군 pack 지침
- 결과 형식 규칙

### 보내지 않는 것
- 전체 원문 파일
- 고객 전체 연락처 목록
- 불필요한 민감 메모

### 안전 장치
- 작업 전 사용자 미리보기
- 민감정보 마스킹 토글
- 로컬 요약 후 전송
- 긴 문서는 섹션별 처리

---

## 10. 라이선스/좌석 보안

### 왜 필요한가
exe 파일은 복사가 쉽기 때문에, 단순 파일 판매는 공유에 취약하다.

### 권장 방식
- 계정 로그인
- 기기 fingerprint 등록
- 좌석 수 제한
- 주기적 서버 재검증
- 관리 콘솔에서 기기 회수 가능

### 예시 정책
- Starter: 1기기
- Pro: 1~2기기
- Office: 3기기

### 추가 옵션
- 기기 변경 월 n회 제한
- 새 기기 승인 알림
- 관리자에게 리셋 권한 부여

---

## 11. 백업/복구 설계

### 로컬 백업
- 사용자가 수동 export 가능
- 암호화된 백업 파일 생성
- USB/외장드라이브 저장 가능

### 선택적 클라우드 백업
- 사용자가 명시적으로 켤 때만 활성화
- 서버에는 암호화된 덩어리로 저장
- 서버 운영자가 내용 복호화 불가하도록 설계 가능

### 복구 시나리오
- 새 PC에서 로그인
- 백업 파일 import
- 로컬 vault 복원
- 필요 시 라이선스 재검증

---

## 12. 운영 정책

### 꼭 기록해야 할 최소 로그
- 로그인 성공/실패
- 기기 등록/해제
- 구독 시작/중단
- 모델 호출 실패
- 데이터 삭제 요청 처리 결과

### 피해야 할 로그
- 문서 원문 전체
- 상담 메모 전체
- 민감 첨부파일 내용

---

## 13. 고객에게 설명할 수 있는 문장

이 제품은 아래처럼 설명하는 것이 좋다.

> "문서와 메모는 내 PC 안에 저장되고, 서버는 로그인과 라이선스만 관리합니다."

이 한 문장은 회계/법률 업종에서 신뢰를 만드는 핵심 문구다.

---

## 14. MVP 우선순위

### 1순위
- 로컬 SQLite
- 파일 vault
- 로그인 / 라이선스
- 좌석 제한
- 기본 백업 export

### 2순위
- 암호화 저장
- 앱 잠금
- 장치 재등록
- 선택적 동기화

### 3순위
- 팀 공유 vault
- 관리자 콘솔
- 감사 로그 확장
- 온프렘 옵션

---

## 15. 결론

이 사업은 **local-first + cloud control plane** 구조가 맞다.

정리하면:
- **민감 데이터는 로컬**
- **인증/결제/라이선스는 서버**
- **LLM은 필요한 최소 정보만 전송**
- **사무실 공유는 좌석/기기 제한으로 통제**

이 구조가 회계사/세무사/변호사 같은 업종에 가장 설득력 있다.
